### Undoing unstagged changes
```git reset <filename>```

### Undoing Commited changes (single commit)
```git reset HEAD~1```

### Undoing multiple commits 
```
git reset <commit hash(link)>
git reset --hard <commit hash>
```